import 'package:flutter/material.dart';

class PhytoButton extends StatelessWidget {
  const PhytoButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PhytoButton')),
      body: const Center(child: Text('PhytoButton')),
    );
  }
}
