import 'package:flutter/material.dart';

class NurseryOrders extends StatelessWidget {
  const NurseryOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('NurseryOrders')),
      body: const Center(child: Text('NurseryOrders')),
    );
  }
}
